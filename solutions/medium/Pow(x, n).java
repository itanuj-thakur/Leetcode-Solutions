// Title: Pow(x, n)
            // Difficulty: Medium
            // Language: Java
            // Link: https://leetcode.com/problems/pow(x,-n)/


    }
        return power(x, N);
        }
            return 1.0 / power(x, -N);
        if (N < 0) {
        long N = n;
        // Handle edge case for integer overflow when n = Integer.MIN_VALUE
    public double myPow(double x, int n) {
class Solution {
